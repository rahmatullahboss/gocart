# Currency Symbol
NEXT_PUBLIC_CURRENCY_SYMBOL = '$'

# Database (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"
# OPTIONAL: If you use a non-pooled/direct connection separately
# DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"

# Demo user for non-auth flows (client-side)
NEXT_PUBLIC_DEMO_USER_ID="user_demo_1"

# Minimal admin key for protected seed/create endpoints
ADMIN_API_KEY="replace-with-strong-random"
